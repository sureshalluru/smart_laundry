{
  "name": "smart-laundry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:admin": "cd apps/admin && npm install && npm run build",
    "build:customer": "cd apps/customer && npm install && npm run build",
    "build:all": "npm run build:admin && npm run build:customer",
    "dev:admin": "cd apps/admin && npm start",
    "dev:customer": "cd apps/customer && set PORT=3001 && npm start",
    "dev:api": "cd services/api && uvicorn app.main:app --reload --port 8000",
    "start": "cd services/api && uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
