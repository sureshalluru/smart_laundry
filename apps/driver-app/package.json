{
  "name": "smart-laundry-driver",
  "version": "1.0.0",
  "description": "Smart Laundry Driver - Background GPS Tracking",
  "private": true,
  "scripts": {
    "build": "echo 'Uses admin build'",
    "cap:sync": "cap sync android",
    "cap:build": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor-community/background-geolocation": "^1.2.26"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
